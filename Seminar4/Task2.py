# Создаем неизменяемую строку
immutable_string = "Hello, World!"

# Попытка изменить строку
# Это вызовет ошибку, так как строки являются неизменяемыми
# immutable_string[0] = 'h'

# Создание неизменяемого списка (кортежа)
immutable_list = (1, 2, 3, 4, 5)

# Попытка изменения элемента
# Это вызовет ошибку, так как кортежи неизменяемы
# immutable_list[2] = 10


